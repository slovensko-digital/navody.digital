<%= content_for :title, build_page_title('Osobné údaje', 'Parlamentné voľby') %>
<%= content_for(:headerline, 'Parlamentné voľby') %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_for @application_form, :builder => AppFormBuilder, url: :delivery_apps_parliament_vote_app_application_forms do |f| %>
      <h1 class="govuk-heading-xl">Vaše osobné údaje</h1>
      <%= f.hidden_field :step %>
      <%= f.hidden_field :sk_citizen %>

      <%= f.text_field :full_name, class: 'govuk-input--width-20', label: 'Meno, priezvisko, titul', hint: 'Ako na občianskom preukaze. Napríklad: JUDr. Ján Mrvička', autocomplete: 'name' %>
      <%= f.text_field :pin, class: 'govuk-input--width-10', label: 'Rodné číslo', hint: 'Napríklad: 123456/1234' %>

      <%= f.submit 'Pokračovať'%>
    <% end %>
  </div>
</div>

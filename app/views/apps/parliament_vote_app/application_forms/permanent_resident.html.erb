<%= content_for :title, build_page_title('Miesto voľby', 'Parlamentné voľby') %>
<%= content_for(:headerline, 'Parlamentné voľby') %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_for @application_form, :builder => AppFormBuilder do |f| %>
      <%= f.hidden_field :step %>
      <%= f.radios :permanent_resident, 'Máte trvalý pobyt na Slovensku?', inline: true do %>
        <%= f.radio_button :permanent_resident, 'yes', label: "Áno" %>
        <%= f.radio_button :permanent_resident, 'no', label: "Nie"%>
      <% end %>

      <%= f.submit 'Pokračovať' %>
    <% end %>
  </div>
</div>

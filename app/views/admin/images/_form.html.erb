<%= form_with(url: "/admin/images", multipart: false, builder: AdminFormBuilder) do |form| %>
  <%= form.file_field :picture %>
  <%= form.text_field :picture_name %>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

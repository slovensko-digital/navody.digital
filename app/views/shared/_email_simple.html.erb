<div class="govuk-form-group<%= ' govuk-input--error' if form.errors[:email].any? %>">
  <%= f.label hash_key(:email, root), 'Emailová adresa', class: 'govuk-label' %>
  <% form.errors[:email].each do |error| %>
    <span class="govuk-error-message"><%= error %></span>
  <% end %>

  <%= f.email_field hash_key(:email, root), class: "govuk-input govuk-input--width-20 #{'govuk-input--error' if form.errors[:email].any?}", autocomplete: :email %>
</div>